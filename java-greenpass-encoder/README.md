# Java Greenpass Encoder

To build the project, run `mvn clean package`. The resulting jar-file will be found in target/.

Then, ` java -jar target/covid-encoder-1.0-SNAPSHOT.jar data_with_keys.txt` will generate the HC1-string.
